{
  "name": "enterprise-carpooling-workspace",
  "version": "1.0.0",
  "description": "Monorepo Workspace for Enterprise Carpooling Platform",
  "private": true,
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install --legacy-peer-deps",
    "install:backend": "cd backend && npm install",
    "install:all": "npm run install:server && npm run install:client && npm run install:backend",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
